# BioVoid Frontend

This directory contains the canonical React/Vite interface for the local
BioVoid research prototype. Build it with `npm run build` and serve the
generated files through the FastAPI application from the repository root.
